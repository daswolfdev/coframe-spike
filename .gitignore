# Secrets — config is committed; secrets are env-loaded (CLAUDE.md house rule)
.env
.env.*

# SQLite runtime state — queue/config/agg dbs and their WAL sidecars
data/
*.db
*.db-wal
*.db-shm

# Python (api service: uv + ruff; uv.lock is config and stays committed)
__pycache__/
*.pyc
.venv/
.ruff_cache/
.pytest_cache/

# Generated by `make smoke` (cleaned up on success; ignore crash residue)
services/smoke-test/

# macOS
.DS_Store
